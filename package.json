{
  "name": "kates-kafes",
  "version": "1.0.0",
  "private": true,
  "description": "Kate's Kafes — a map of Park Slope coffee shops",
  "scripts": {
    "build": "node scripts/generate-config.js",
    "start": "node scripts/dev-server.js",
    "generate-data": "python3 scripts/generate-data.py",
    "discover": "node scripts/discover-cafes.js"
  }
}
